allOf:
  - $ref: '#/components/schemas/BasicObject'
  - properties:
      payment-method:
        type: string
        description: |
          Payment method.
        enum:
          - CARD
        example: CARD
    required:
      - payment-method
