post:
  summary: Create and send invoice
  description: |
    Collect invoice data, send to stripe, create Stripe invoice and send it.
  tags:
    - Stripe Payments
  responses:
    200:
      description: Success
    default:
      description: Unexpected error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
